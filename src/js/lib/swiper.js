import Swiper from 'swiper';

new Swiper ('.swiper-container', {
  loop: true,
  // Navigation arrows
  navigation: {
    nextEl: '.next',
    prevEl: '.prev',
  },
});
new Swiper ('.swiper-container-hello', {
  loop: true,
  // Navigation arrows
  navigation: {
    nextEl: '.next',
    prevEl: '.prev',
  },
});
